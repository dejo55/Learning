Module ClassLearning
    Sub main()

        Dim rect As New CRectangle
        Dim rect2 As New CRectangle
        Dim cube As New Ccube

        ' rect2.width = CInt(Console.ReadLine())

        ' rect2.Hight = CInt(Console.ReadLine())
        ' cube.Deepth = CInt(Console.ReadLine())
        'rect.Width = 10

        ' rect.hight = 20

        'Console.WriteLine("The area of the rectangle is " & cube.getArea())
        Console.WriteLine("The area of the new rectangle is " & cube.getArea())

        Console.ReadKey()

    End Sub
End Module
